/**
 * Общие длительности анимаций и переходов, мс.
 * В CSS доступны как переменные `--duration-*` (см. `src/styles/cssVariables.ts`).
 * Тайминги сцены intro — отдельно, в `introTimings.ts`.
 */
export const TIMINGS = {
  /** Появление и скрытие проигрывателя в углу экрана. */
  turntableFadeMs: 600,
  /** Отклик интерактивных элементов на hover и focus. */
  interactionFeedbackMs: 150,
} as const;

/** Общий переход между сценами (`SceneTransitionClouds`), мс. */
export const SCENE_TRANSITION_TIMINGS = {
  /** Облака съезжаются и закрывают экран. */
  coverMs: 1500,
  /** Облака расходятся и показывают новую сцену. */
  revealMs: 1400,
  /** Задержка между соседними облаками одной группы. */
  staggerMs: 110,
} as const;

/**
 * Небо за облаками в любом облачном переходе, мс: за это время оно проявляется в самом начале
 * закрытия и гаснет в самом конце раскрытия. Пока облака в кадре, за ними видно небо, а не сцена.
 */
export const CLOUD_SKY_FADE_MS = 500;

/** Кривые анимаций GSAP. */
export const EASINGS = {
  clouds: 'power2.inOut',
  /** Небо за облаками: быстро проявляется при закрытии и уходит в самом конце раскрытия. */
  skyAppear: 'sine.out',
  skyVanish: 'sine.in',
  fade: 'power1.inOut',
  dock: 'power3.inOut',
  pulse: 'sine.inOut',
  /** Поворот тонарма вокруг шарнира: плавный старт и мягкая «посадка» иглы. */
  tonearm: 'power2.inOut',
  /** Разгон пластинки до рабочей скорости. */
  spinUp: 'power1.in',
  /** Постоянное вращение пластинки. */
  linear: 'none',
  /** «Дыхание» и задувание огня свечи: мягкие органичные колебания. */
  flame: 'sine.inOut',
  /** Подъём струйки дыма: быстрый старт, плавное замедление. */
  smokeRise: 'sine.out',
  /** Проявление и растворение дыма. */
  smokeAppear: 'sine.out',
  smokeVanish: 'sine.in',
  /** Зум стадии сада в сцене Desk: мягкий, как наезд камеры. */
  gardenZoom: 'sine.inOut',
  /**
   * Перетекание стадий Desk: следующая картинка проявляется поверх текущей, а текущая гаснет
   * за то же время, но большую часть его остаётся почти непрозрачной. Иначе в середине
   * перетекания обе картинки полупрозрачны и сквозь них просвечивает фон сцены.
   */
  crossfadeIn: 'sine.inOut',
  crossfadeOut: 'power3.in',
} as const;
