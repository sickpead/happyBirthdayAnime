/**
 * Громкости (0–1) и длительности (мс) фоновой музыки: очередь дорожек живёт в
 * `src/audio/playlist.ts` и звучит с первой сцены до конца сессии. Громкости самого
 * вступления (старт песни и её выход на основной уровень) — в `introTimings.ts`.
 */

/** Фоновая громкость после второй сцены: музыка остаётся, но уходит на задний план. */
export const BACKGROUND_MUSIC_VOLUME = 0.32;

/** Переход на фоновую громкость при входе в сцену стола — медленный, его не слышно. */
export const BACKGROUND_MUSIC_FADE_MS = 2600;

/** Дорожка, выбранная гостем в стопке пластинок: её слушают, а не держат фоном. */
export const SELECTED_TRACK_VOLUME = 0.8;

/** Пересменка дорожек очереди: предыдущая уходит, следующая нарастает за то же время. */
export const TRACK_CROSSFADE_MS = 1200;
