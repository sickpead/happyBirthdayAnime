/**
 * Тайминги (мс) и формы анимаций торта: горение огней, задувание и дым.
 * Массивы по свечам идут слева направо; если свечей больше, значения берутся по кругу.
 * Где стоят сами свечи и на сколько сдвинут старт «дыхания» каждой — в `cakeCandles.ts`.
 */

/* ── Горение ──────────────────────────────────────────────────────────── */

/** Цикл «дыхания» каждого огня — у каждой свечи свой, чтобы огни не шли в такт. */
export const FLAME_CYCLE_MS = [1000, 1120, 920, 1060, 960] as const;

/**
 * Форма «дыхания» огня за один цикл: значения распределяются по циклу равномерно,
 * первое и последнее совпадают — цикл замыкается без рывка. Разное число точек
 * у свойств даёт несовпадающий ритм, и пламя не выглядит механическим.
 */
export const FLAME_FLICKER_KEYFRAMES = {
  scaleY: [1, 1.1, 0.96, 1.06, 1],
  scaleX: [1, 0.9, 1.08, 0.94, 1],
  rotation: [0, -2, 3, -3, 2, 0],
  opacity: [1, 0.94, 1, 0.96, 1],
} as const;

/* ── Задувание ────────────────────────────────────────────────────────── */

/** Интервал между задуванием соседних свечей (flame-1 → flame-5). */
export const BLOW_OUT_STAGGER_MS = 180;

/**
 * Огонь отклоняется от «дуновения», сжимается и гаснет. Каждый кадр анимирует
 * от текущей формы огня, поэтому задувание подхватывает пламя с любой фазы горения.
 * Сумма длительностей — 450 мс.
 */
export const BLOW_OUT_KEYFRAMES = [
  { rotation: -8, scaleX: 0.75, scaleY: 1.05, opacity: 0.9, durationMs: 140 },
  { rotation: 5, scaleX: 0.65, scaleY: 0.72, opacity: 0.6, durationMs: 140 },
  { rotation: -3, scaleX: 0.55, scaleY: 0.35, opacity: 0, durationMs: 170 },
] as const;

/* ── Дым ──────────────────────────────────────────────────────────────── */

/** Дым появляется из фитиля, пока огонь догорает: от начала задувания этой свечи. */
export const SMOKE_DELAY_MS = 300;
/** Подъём и растворение струйки. */
export const SMOKE_DURATION_MS = 1300;
/** Проявление до пика прозрачности (остаток длительности — растворение). */
export const SMOKE_FADE_IN_MS = 350;
/** Пиковая непрозрачность дыма — лёгкий и ненавязчивый. */
export const SMOKE_PEAK_OPACITY = 0.7;
/** Масштаб струйки в начале и в конце подъёма. */
export const SMOKE_SCALE_FROM = 0.5;
export const SMOKE_SCALE_TO = 1.15;
/**
 * Подъём струйки, % её высоты: на экране высотой 1080 px это ≈ 25 px, на меньших экранах
 * подъём уменьшается вместе с тортом.
 */
export const SMOKE_RISE_Y_PERCENT = -25;
/** Боковой снос каждой струйки, % её ширины (знак — направление). */
export const SMOKE_DRIFT_X_PERCENT = [-18, 14, -10, 16, -14] as const;

/* ── Закрытие облаков и пожелание ─────────────────────────────────────── */

/** Поздравление и кнопка тают, когда облака съезжаются. */
export const CAKE_CONTENT_FADE_MS = 600;
/** Облака съезжаются к центру и закрывают экран. */
export const CAKE_CLOUD_CLOSE_DURATION_MS = 1500;
/** Задержка между соседними облаками одной группы при закрытии. */
export const CAKE_CLOUD_CLOSE_STAGGER_MS = 110;
/** Пожелание проявляется на уже закрывающихся облаках. */
export const CAKE_WISH_FADE_IN_MS = 800;
/** Пауза с пожеланием, прежде чем уйти на стол. */
export const CAKE_WISH_HOLD_MS = 2800;
