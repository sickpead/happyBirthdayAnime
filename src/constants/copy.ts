/**
 * Тексты интерфейса. Собраны в одном месте, чтобы их было легко вычитывать
 * и при необходимости локализовать.
 *
 * Тексты dev-панели сюда намеренно не входят: они живут в `src/dev/`, чтобы не попадать
 * в production-бандл (свойства общего объекта tree-shaking не вырезает).
 */
export const UI_TEXT = {
  sceneTitle: (sceneLabel: string): string => `Сцена: ${sceneLabel}`,
  nextButton: 'Далее →',
  sceneLoading: 'Загружаем сцену…',
  sceneLoadError: 'Не удалось загрузить сцену.',
  reloadPage: 'Перезагрузить страницу',

  closeModal: 'Закрыть',
  modalPlaceholder: (modalTitle: string): string => `TODO: содержимое модалки «${modalTitle}»`,
} as const;

/** Тексты сцены Cake. */
export const CAKE_TEXT = {
  blowOut: 'Задуть свечу',
  /** Доступное имя торта для скринридеров. */
  cakeLabel: 'Праздничный торт с надписью «Арайкас, Туған күніңмен!» и пятью свечами',
} as const;

/** Тексты сцены Desk. */
export const DESK_TEXT = {
  /** Описания картинок стадий для скринридеров. */
  stageAlt: {
    wide: 'Дом с садом среди полей с высоты птичьего полёта, сквозь облака; во дворике едва виден столик',
    medium:
      'Дом с садом ближе, в тёплом закатном свете: вокруг деревня и поля, во дворике столик со стулом',
    close:
      'Дом вплотную: у крыльца среди цветов столик со стулом, на нём торт, книга и проигрыватель',
    empty: 'Пустой деревянный стол со стулом в саду, крупным планом',
    hub: 'Большой деревянный стол в саду: торт, книга, черновики, проигрыватель и письмо на стуле',
  },
  /** Доступное имя группы предметов на столе. */
  objectsLabel: 'Предметы на столе',
  /** Подсказка на плейсхолдере, пока картинки стадии нет. */
  missingImage: (filePath: string): string => `Нет файла ${filePath}`,
} as const;

/** Тексты читалки комикса в модалке «Книга». */
export const COMIC_TEXT = {
  /** Доступное имя листалки. */
  readerLabel: 'Страницы комикса',
  previousPage: 'Предыдущая страница',
  nextPage: 'Следующая страница',
  /** Счётчик страниц под картинкой. */
  pageCounter: (page: number, total: number): string => `${String(page)} / ${String(total)}`,
  /** Описание страницы для скринридеров. */
  pageAlt: (page: number, total: number): string =>
    `Страница комикса ${String(page)} из ${String(total)}`,
  /** Подсказка, если страниц ещё нет. */
  empty: 'Страниц пока нет: положите файлы в public/assets/comic/',
  /** Подсказка про приближение страницы. */
  zoomHint: 'Колесо мыши — приблизить страницу',
  /** Текущий масштаб приближённой страницы. */
  zoomLevel: (zoom: number): string => `×${zoom.toFixed(1)} · перетащите мышью`,
  resetZoom: 'Показать целиком',
} as const;

/** Тексты стопки пластинок: список дорожек в модалке. */
export const VINYL_TEXT = {
  /** Доступное имя списка дорожек. */
  listLabel: 'Дорожки',
  /** Подсказка над списком. */
  hint: 'Нажмите на дорожку, чтобы включить её',
  /** Что делает клик по дорожке — доступное имя кнопки. */
  play: (title: string): string => `Включить «${title}»`,
  stop: (title: string): string => `Остановить «${title}»`,
  /** Пометка играющей дорожки. */
  nowPlaying: 'играет',
  /** Подсказка, если дорожек ещё нет. */
  empty: 'Дорожек пока нет: положите файлы в public/assets/audio/vinyl/',
} as const;

/** Тексты сцены intro. */
export const INTRO_TEXT = {
  greeting: 'С Днём Рождения, Арайкош!',
  invite: 'нажми, чтобы продолжить',
  /** Доступное имя проигрывателя для скринридеров. */
  turntableLabel: 'Проигрыватель: тонарм опускает иглу на пластинку, и она начинает вращаться',
} as const;
