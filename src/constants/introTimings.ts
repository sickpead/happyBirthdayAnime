/**
 * Тайминги (мс) и громкости (0–1) сцены intro. Значения звука и облаков проверены в прототипе.
 * Порядок констант повторяет сценарий: открытие → проигрыватель → тонарм → музыка → закрытие.
 */

/* ── a. Раскрытие облаков по клику ─────────────────────────────────────── */

/** Облака разъезжаются в стороны. */
export const CLOUD_REVEAL_DURATION_MS = 1400;
/** Задержка между соседними облаками одной группы при раскрытии. */
export const CLOUD_REVEAL_STAGGER_MS = 110;
/** Надпись-поздравление тает одновременно с раскрытием облаков. */
export const GREETING_FADE_OUT_MS = 800;

/* ── b–e. Проигрыватель: пауза и тонарм ───────────────────────────────── */

/**
 * Пауза после раскрытия: проигрыватель уже стоит на сцене (он не проявляется),
 * винил неподвижен, тонарм поднят — сцена просто дышит.
 */
export const WAIT_AFTER_REVEAL_MS = 2200;
/** Пауза перед тем, как тонарм двинется к пластинке. */
export const NEEDLE_DROP_DELAY_MS = 1200;
/** Тонарм плавно поворачивается вокруг шарнира от поднятого положения к пластинке. */
export const TONEARM_MOVE_DURATION_MS = 1800;

/* ── f. Игла коснулась пластинки: вращение и звук ─────────────────────── */

/** Период одного оборота пластинки: 60 000 мс / 33⅓ об/мин = 1800 мс. */
export const VINYL_SPIN_MS_PER_ROTATION = 1800;
/** Разгон пластинки от покоя до рабочей скорости после касания иглы. */
export const VINYL_SPIN_UP_MS = 900;
/** Громкость звука опускания иглы (без фейда — короткий транзиент). */
export const NEEDLE_DROP_SFX_VOLUME = 0.9;
/** Целевая громкость винилового треска. */
export const CRACKLE_TARGET_VOLUME = 0.5;
/** Нарастание громкости треска от 0 до целевой. */
export const CRACKLE_FADE_IN_MS = 900;

/* ── g–h. Песня ───────────────────────────────────────────────────────── */

/**
 * Задержка старта песни после касания иглы (0 — строго одновременно с вращением).
 * Песня — первая дорожка фоновой очереди (`src/audio/playlist.ts`): она не принадлежит
 * сцене и после вступления продолжает играть.
 */
export const SONG_START_DELAY_MS = 1400;
/** Громкость, с которой песня начинает звучать: сперва тихо, будто издалека. */
export const SONG_START_VOLUME = 0.2;
/** Основная громкость песни во вступлении. */
export const SONG_TARGET_VOLUME = 0.85;
/** Выход песни со стартовой громкости на основную. */
export const SONG_FADE_IN_MS = 3000;
/** Сцена держится после старта песни — ничего не происходит, играет музыка. */
export const SCENE_HOLD_MS = 7000;

/* ── i–j. Закрытие и уход проигрывателя в угол ────────────────────────── */

/**
 * Затухание звуков сцены (виниловый треск) вместе с закрытием облаков. Музыки это не
 * касается: она идёт дальше и на столе уходит на фоновую громкость (`audioMix.ts`).
 */
export const CLOSE_AUDIO_FADE_MS = 2200;
/** Облака съезжаются к центру и закрывают экран. */
export const CLOUD_CLOSE_DURATION_MS = 1500;
/** Задержка между соседними облаками одной группы при закрытии. */
export const CLOUD_CLOSE_STAGGER_MS = 110;
/** Задержка ухода проигрывателя в угол от начала закрытия. */
export const SHRINK_TO_CORNER_DELAY_MS = 300;
/** Проигрыватель уезжает и уменьшается в правый верхний угол. */
export const SHRINK_TO_CORNER_DURATION_MS = 1200;

/* ── Приглашение «нажми, чтобы продолжить» ────────────────────────────── */

/** Половина цикла пульсации (затухание или проявление). */
export const INVITE_PULSE_HALF_CYCLE_MS = 1100;
/** Минимальная непрозрачность в пульсации. */
export const INVITE_PULSE_MIN_OPACITY = 0.45;
/** Максимальный масштаб в пульсации. */
export const INVITE_PULSE_SCALE = 1.06;
